services:
  - type: web
    name: money-monitor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web.py
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
